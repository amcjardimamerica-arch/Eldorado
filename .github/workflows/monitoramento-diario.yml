name: 01 · Eldorado — monitoramento diário às 6h
on:
  schedule:
    - cron: "0 9 * * *" # 06:00 America/Sao_Paulo (UTC-3)
  workflow_dispatch:
permissions:
  contents: write
  issues: write
concurrency:
  group: eldorado-repo-writes
  cancel-in-progress: false
jobs:
  monitorar:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - name: Testes antes da coleta
        run: python -m unittest discover -s tests -v
      - name: Coletar, verificar pistas e publicar painel Eldorado
        run: python -m src.executar_diario
      - name: Registrar somente alterações produzidas
        run: |
          git config user.name "eldorado-bot"
          git config user.email "eldorado-bot@users.noreply.github.com"
          git add dados/oportunidades dados/financiadores estado docs
          git diff --cached --quiet && exit 0
          git commit -m "dados: monitoramento diário $(date -u +%F)"
          git push
